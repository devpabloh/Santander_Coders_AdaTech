// Switch / case é muito bom quando sua variável possui valores específicos.

const permissões = 'Aluno' // pode receber "aluno" || "professor" || "administrador"

switch(variavel){
    case
    break
}